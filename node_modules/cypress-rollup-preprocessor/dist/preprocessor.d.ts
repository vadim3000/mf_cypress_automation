/// <reference types="node" />
import { EventEmitter } from 'events';
import { OutputOptions, InputOptions } from 'rollup';
export declare type FileObject = EventEmitter & {
    filePath: string;
    outputPath: string;
    shouldWatch: boolean;
};
export interface PreprocessorOptions {
    inputOptions?: Partial<InputOptions>;
    outputOptions?: Partial<OutputOptions>;
}
export declare function preprocessor(options?: PreprocessorOptions): (file: FileObject) => Promise<string>;
